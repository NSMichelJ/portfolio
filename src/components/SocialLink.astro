---
    import type { HTMLAttributes } from 'astro/types';
    
    interface Props extends HTMLAttributes<'a'> {
    }
---

<a class={`text-sm text-center font-medium px-3 py-1.5 rounded-lg inline-flex items-center ${Astro.props.class}`} {...Astro.props}>
    <slot />
</a>