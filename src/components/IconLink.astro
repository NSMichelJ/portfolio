---
    import type { HTMLAttributes } from 'astro/types';
    
    interface Props extends HTMLAttributes<'a'> {
    }
---

<a class={`text-2xl text-center flex items-center justify-center w-8 h-8 bg-white rounded-full hover:scale-125 ease-in-out duration-150 ${Astro.props.class}`} {...Astro.props}>
    <slot />
</a>